# LLM API Key Checker Extension

![Icon](frontend/public/icon.png)

一个现代化的、完全本地运行的浏览器扩展，用于批量检测各大 LLM 提供商（OpenAI, Anthropic, DeepSeek, Gemini, etc.）的 API Key 有效性及余额。

> **隐私承诺**：本工具所有检测请求均通过您的本机浏览器直接发起，**绝不通过任何中间服务器**，Key 数据仅保存在您的本地存储中。

## ✨ 功能特性

- 🔒 **本地安全**：零后端架构，直接与官方 API 通信，确保 Key 不泄露。
- 🚀 **批量极速**：支持粘贴多行 Key，自定义并发数进行高速检测。
- 💰 **余额查询**：支持显示 API Key 的剩余余额、总额度及过期时间（视具体厂商 API 支持情况）。
- 📝 **历史记录**：自动保存检测结果，支持一键重新加载配置或复制有效 Key。
- 🎨 **精美 UI**：基于 "Anthropic Aesthetic" 设计风格，支持明亮/暗色主题。
- 🧩 **全平台支持**：支持 OpenAI, Anthropic (Claude), DeepSeek, Gemini, OpenRouter, SiliconFlow, Moonshot, Tavily 等多种服务商。

## 📦 安装说明

### 方式一：加载已解压的扩展程序（推荐开发使用）

1. 下载本项目源码或 Release 包。
2. 打开 Chrome/Edge 浏览器，访问 `chrome://extensions`。
3. 开启右上角的 **开发者模式 (Developer mode)**。
4. 点击 **加载已解压的扩展程序 (Load unpacked)**。
5. 选择本项目下的 `dist` 文件夹（如果从源码运行，请先参考下文进行构建）。

## 🛠️ 本地开发与构建

### 环境要求

- Node.js >= 16
- NPM 或 Yarn

### 步骤

1. 克隆项目：

   ```bash
   git clone https://github.com/YourUsername/llm-api-key-checker-extension.git
   cd llm-api-key-checker-extension
   ```

2. 安装依赖：

   ```bash
   npm install
   ```

3. 启动开发服务器（Web页面预览模式）：

   ```bash
   npm run dev
   ```

   *注意：`npm run dev` 仅启动网页版，不包含浏览器扩展特有的 `chrome.*` API，数据将存储在 localStorage 中。*

4. **构建插件**：

   ```bash
   npm run build
   ```

   构建产物将输出到 `dist/` 目录。每次修改代码后都需要运行此命令。

## 📂 目录结构

```
e:\llm-api-key-checker-extension\
├── dist/                # 构建产物（加载到浏览器扩展的目录）
├── frontend/            # 前端源码 (Vue 3 + Vite)
│   ├── public/          # 静态资源 (manifest.json, icons)
│   ├── src/
│   │   ├── components/  # Vue 组件
│   │   ├── stores/      # Pinia 状态管理
│   │   ├── utils/       # 工具函数 (API请求, 存储封装)
│   │   ├── App.vue      # 主应用入口
│   │   └── main.js      # 入口文件
│   └── vite.config.js   # Vite 配置
├── .gitignore
├── package.json
└── README.md
```

## 🤝 贡献

欢迎提交 Issues 和 Pull Requests！

## 📄 许可证

MIT License
